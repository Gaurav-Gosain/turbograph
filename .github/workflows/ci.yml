name: ci

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: test (${{ matrix.os }}, go ${{ matrix.go }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        go: [stable]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v6
        with:
          go-version: ${{ matrix.go }}
          cache: true

      - name: build
        run: go build ./...

      - name: build (pure stdlib, no SIMD)
        run: go build -tags noasm ./...

      - name: test
        run: go test -short ./...

      - name: test (race detector)
        if: matrix.os == 'ubuntu-latest' && matrix.go == 'stable'
        run: go test -short -race ./...

  vet-build:
    name: vet and build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v6
        with:
          go-version: stable
          cache: true
      - name: go vet
        run: go vet ./...
      - name: go build
        run: go build ./...

  lint:
    name: golangci-lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v6
        with:
          go-version: stable
          cache: true
      # Install golangci-lint from source with this repo's Go toolchain. The
      # prebuilt action binaries are compiled with an older Go and refuse to lint
      # a module whose go directive (1.26) is newer than their build toolchain.
      - name: install golangci-lint
        run: go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@latest
      - name: golangci-lint
        run: golangci-lint run --timeout=5m ./...

  benchmarks:
    name: benchmarks smoke
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v6
        with:
          go-version: stable
          cache: true
      - name: build and run benchmarks once
        run: go test -run=^$ -bench=. -benchtime=1x ./...

  govulncheck:
    name: govulncheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v6
        with:
          go-version: stable
          cache: true
      - name: govulncheck
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          govulncheck ./...

  clients:
    name: client tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: python client tests
        working-directory: clients/python
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
          python -m pytest -q

      - name: set up Node
        uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: clients/typescript/package-lock.json

      - name: typescript client tests
        working-directory: clients/typescript
        run: |
          npm ci
          npx tsc --noEmit
          npm test
